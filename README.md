# Daily Control Tower

A local-first daily routine planner built with React and Vite.

## Run

```powershell
pnpm install
pnpm dev
```

Planner data is stored in the browser under `daily-control-tower:v1`. Use the menu in the header to export or import a JSON backup.
